1. git status #перевірка статусу
2.git commit -m""  #додає коментар до ПР
git add .    #додає всі файли в репозиторій

git merge new_branch  #????

git pull origin main #затягнути собі

git branch #перевірка яка гілка розробки активна

git checkout -b Lesson.... #після мінус б пишемо назву гілки і воно створює нову гілку